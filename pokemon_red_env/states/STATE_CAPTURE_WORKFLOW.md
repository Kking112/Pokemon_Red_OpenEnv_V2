# State Capture Workflow (Deferred)

State capture is intentionally deferred in this phase.

Current implementation uses existing states in `pokemon_red_env/states` and does not create new states yet.

Future workflow can be added here when custom curriculum-specific states are required.
